This Python script has a function named generate_training_images . as name shows it generate n no of images used as training data 
for machine learning model.

takes three Args:-
output_dir = the directory in which all the labels is saved 
pose_name = the folder name used as a label for model
num_images = how many image data you want to capture


how to use this functio:-
aAfter giving three Args value , Now click "S"  to capture image .
when it is done it automatically terminate or if you want to do in the middle you can press "Q" .